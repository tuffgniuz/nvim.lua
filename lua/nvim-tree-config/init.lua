require("nvim-tree").setup({
	-- auto_close = true,
	diagnostics = {
		enable = true,
	},
    filters = {
        dotfiles = true,
    }
	--[[ view = { ]]
	--[[ 	auto_resize = true, ]]
	--[[ }, ]]
})
